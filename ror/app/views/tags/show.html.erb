<% content_for(:title, t(".title", tag: @tag.name)) %>

<h1><%= t(".title", tag: @tag.name) %> <span class="title-more"><%= link_to(t("tags.back_all"), tags_path) %></span></h1>

<%= render "shared/book_grid", reviews: @reviews %>