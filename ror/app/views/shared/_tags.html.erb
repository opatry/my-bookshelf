<%# locals: tags (Tag collection) %>
<% if tags.any? %>
  <ul class="book-tags">
    <li><span aria-hidden="true">🏷️</span></li>
    <% tags.each do |tag| %>
      <li class="tag"><%= link_to tag.name, tag_path(tag) %></li>
    <% end %>
  </ul>
<% end %>