<%
require 'json'

books_json = @items.find_all { |item| book?(item) }
                    .sort_by { |book| sortable_label(book[:title]) }
                    .map { |book| to_json(book, url: :relative) }
%><%= books_json.to_json %>